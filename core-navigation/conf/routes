# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# For dev machines
GET    /assets/*file                    controllers.Assets.at(path="/public", file)
GET    /most-viewed                     controllers.MostPopularController.renderGlobal()
GET    /most-viewed.json                controllers.MostPopularController.renderGlobalJson()
GET    /most-viewed/*path.json          controllers.MostPopularController.renderJson(path)
GET    /most-viewed/*path               controllers.MostPopularController.render(path)
GET    /top-stories                     controllers.TopStoriesController.render()
GET    /top-stories.json                controllers.TopStoriesController.renderJson()
GET    /related/*path                   controllers.RelatedController.render(path)