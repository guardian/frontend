// sets up global config data
guardian = {
    config: {
        beaconUrl: '',
        page: {
            tones: 'foo',
            series: 'bar',
            references: [{baz: 'one'}, {baz: 'two'}],
            webPublicationDate: '2013-03-20T17:07:00.000Z'
        }
    }
};

define([], function () {
    // do nothing
    return {};
});
