define(['common', 'bonzo', 'bean'], function (common, bonzo, bean) {
    
    return {
        
        init: function() {
            // find all messages
            var messages = common.$g('.message')
            // add close button
        }
        
    }

});