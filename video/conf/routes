# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /pages/*path                controllers.VideoController.render(path, format = "html")
GET     /pages/*path.:format        controllers.VideoController.render(path, format)

# For dev machines
GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /*path                      controllers.VideoController.render(path, format = "html")
GET     /*path.:format              controllers.VideoController.render(path, format)
