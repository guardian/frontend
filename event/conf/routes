# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# For dev machines
GET    /assets/*file                              controllers.Assets.at(path="/public", file)

GET    /story-package/chronological01/*contentId  controllers.StoryPackageController.version1List(contentId)
GET    /story-package/chronological02/*contentId  controllers.StoryPackageController.version2List(contentId)
GET    /story-package/tone-group/*contentId       controllers.StoryPackageController.toneGroupList(contentId)
GET    /story-package/timeline/*contentId       controllers.StoryPackageController.toneGroupList(contentId)

GET    /event/timeline/*eventId                   controllers.EventController.timeline(eventId)