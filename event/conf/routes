# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# For dev machines
GET    /assets/*file                              controllers.Assets.at(path="/public", file)

#GET    /story/package/*contentId                 controllers.StoryController.withContent(contentId)

GET    /story/timeline/*id                        controllers.StoryController.byId(id)
GET    /story/*id                                 controllers.StoryController.byId(id)
