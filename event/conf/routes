# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# For dev machines
GET    /assets/*file                              controllers.Assets.at(path="/public", file)

#GET    /story/with-content/*contentId           controllers.StoryController.withContent(contentId)
GET    /story/id/*id                            controllers.StoryController.byId(id)
