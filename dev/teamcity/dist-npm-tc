#!/bin/bash

set -o xtrace
set -o nounset
set -o errexit

echo "Use project Node version"
$NVM_DIR/nvm.sh
nvm install

npm -g install grunt-cli

echo "Install dependencies"
make install

./grunt-tc clean
