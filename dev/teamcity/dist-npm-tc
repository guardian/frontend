#!/bin/bash
set -o xtrace
set -o errexit

. $NVM_DIR/nvm.sh
echo "Use project Node version"
nvm install

set -o nounset

npm -g install grunt-cli

echo "Install dependencies"
make install

./grunt-tc clean
