#!/bin/bash

. $NVM_DIR/nvm.sh

set -o xtrace
set -o nounset
set -o errexit

echo "Use project Node version"
nvm install

npm -g install grunt-cli

echo "Install dependencies"
make install

./grunt-tc clean
