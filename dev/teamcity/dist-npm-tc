#!/bin/bash
. $NVM_DIR/nvm.sh
echo "Use project Node version"
nvm install

set -o xtrace
set -o errexit
set -o nounset

npm -g install grunt-cli

echo "Install dependencies"
make install

./grunt-tc clean
