/**
 * DO NOT EDIT THIS FILE
 *
 * It is not used to to build anything.
 *
 * It's just a record of the old flow types.
 *
 * Use it as a guide when converting
 * - static/src/javascripts/projects/common/modules/identity/sign-in-gate/types.js
 * to .ts, then delete it.
 */

// @flow

export type CurrentABTest = {
    name: string,
    variant: string,
};

export type ComponentEventParams = {
    componentType: string,
    componentId?: string,
    abTestName: string,
    abTestVariant: string,
    viewId: string,
    browserId?: string,
    visitId?: string,
};

export type DismissalWindow = 'day' | 'dev';
export type GateStatus = boolean | 'dismissed' | 'signed in';

export type SignInGateVariant = {
    show: ({
        abTest: CurrentABTest,
        guUrl: string,
        signInUrl: string,
        ophanComponentId: string,
    }) => boolean,
    canShow: (name?: string) => boolean,
    name: string,
};
