/**
 * DO NOT EDIT THIS FILE
 *
 * It is not used to to build anything.
 *
 * It's just a record of the old flow types.
 *
 * Use it as a guide when converting
 * - static/src/javascripts/projects/common/modules/identity/ad-prefs/FeedbackFlashBox.js
 * to .ts, then delete it.
 */

// @flow
import React, { Component } from 'preact-compat';

type FeedbackFlashBoxProps = {
    children: any,
    flashing: boolean,
};

class FeedbackFlashBox extends Component<FeedbackFlashBoxProps> {
    render() {
        return (
            <div
                className={[
                    'identity-ad-prefs-manager__flash',
                    this.props.flashing
                        ? 'identity-ad-prefs-manager__flash--flashing'
                        : '',
                ].join(' ')}
                aria-hidden="true">
                {this.props.children}
            </div>
        );
    }
}

export { FeedbackFlashBox };
