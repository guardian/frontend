/**
 * DO NOT EDIT THIS FILE
 *
 * It is not used to to build anything.
 *
 * It's just a record of the old flow types.
 *
 * Use it as a guide when converting
 * - static/src/javascripts/projects/common/views/svg.spec.js
 * to .ts, then delete it.
 */

// @flow
import { addClassesAndTitle } from './svg';

describe('svg.addClassesAndTitle', () => {
    test('leaves markup alone if only markup passed', () => {
        expect(addClassesAndTitle('<span></span>')).toBe('<span></span>');
    });

    test('adds all classes passed in', () => {
        expect(
            addClassesAndTitle('<span class="foo"></span>', ['bar', 'baz'])
        ).toBe('<span class="bar baz foo"></span>');
    });

    test('adds title if present', () => {
        expect(
            addClassesAndTitle(
                '<span class="foo"></span>',
                ['bar', 'baz'],
                'the title'
            )
        ).toBe('<span title="the title" class="bar baz foo"></span>');
    });
});
