/**
 * DO NOT EDIT THIS FILE
 *
 * It is not used to to build anything.
 *
 * It's just a record of the old flow types.
 *
 * Use it as a guide when converting
 * - static/src/javascripts/projects/commercial/modules/messenger/send.js
 * to .ts, then delete it.
 */

// @flow
import { postMessage } from 'commercial/modules/messenger/post-message';

/* eslint no-bitwise: "off" */

const send = (type: string, payload: any): string => {
    const msg = {
        id: 'xxxxxxxxxx'.replace(/x/g, () =>
            ((Math.random() * 36) | 0).toString(36)
        ),
        iframeId: window.name,
        type,
        value: payload,
    };

    postMessage(msg, window.top, '*');

    return msg.id;
};

export { send };
