# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# This is a bake off, hence two implementations
GET     /resize/sclr/*path    controllers.ImageController.render(path, mode: String = "scalr")
GET     /resize/im4j/*path    controllers.ImageController.render(path, mode: String = "im4java")

