# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /resize/:width/:height/:compression/*path    controllers.ImageController.render(path, width: Int, height: Int, compression: Int)

