# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /pages/*path                controllers.GalleryController.render(path)

# For dev machines
GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /*path                      controllers.GalleryController.render(path)
