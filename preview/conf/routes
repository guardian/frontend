# Paths specific to "preview"
GET     /_healthcheck               controllers.HealthCheck.healthCheck()
GET     /responsive-viewer/*path    controllers.ResponsiveViewerController.preview(path)

#import routes from "standalone" project
->  / standalone.Routes
